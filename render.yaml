services:
  - type: web
    name: mcq-quiz-web
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mcq-quiz-db
          property: connectionString

databases:
  - name: mcq-quiz-db
    plan: free
